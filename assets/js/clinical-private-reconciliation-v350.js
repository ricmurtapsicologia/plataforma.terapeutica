import {data,runtime,nowISO} from './state.js';
import {decryptJson,encryptJson} from './crypto.js';
import {bulkPutEncryptedAtomic,deleteRecord} from './database.js';
import {normalizeIdentity} from './clinical-intake-core-v310.mjs';

const VERSION='3.6.0';
const TOKEN_STORAGE='rm.google.calendar.token.v1';
const CATALOG_STORAGE='rm.google.clinical.reconcile.v230';
const CATALOG_AAD='google-clinical-reconcile-v230';
const DRIVE_SCOPE='https://www.googleapis.com/auth/drive.readonly';
const PATCH_PREFIX='RM Clinical Reconciliation Patch';
const APPLIED_KEY='rm.private.clinical.patch.applied.v1';
const LINK_STORES=['appointments','records','notes','formulations','goals','tasks','materials','documents','payments','consents','communications'];
const arr=v=>Array.isArray(v)?v.filter(Boolean):[];
let running=false;
function norm(v=''){return normalizeIdentity(String(v||''))}
function minuteOf(v=''){const m=String(v||'').match(/^(\d{1,2}):(\d{2})/);return m?Number(m[1])*60+Number(m[2]):null}
function linkedCount(id){return LINK_STORES.reduce((n,s)=>n+arr(data[s]).filter(x=>x?.patientId===id).length,0)}
function identity(p){return{cpf:String(p?.cpf||'').replace(/\D/g,''),email:String(p?.email||'').trim().toLowerCase(),phone:String(p?.phone||'').replace(/\D/g,'')}}
function conflict(a,b){const x=identity(a),y=identity(b);return Boolean((x.cpf&&y.cpf&&x.cpf!==y.cpf)||(x.email&&y.email&&x.email!==y.email)||(x.phone.length>=10&&y.phone.length>=10&&x.phone!==y.phone))}
function namesOf(p){return new Set([norm(p?.name),norm(p?.preferredName)].filter(Boolean))}
function candidates(names=[]){const wanted=new Set(arr(names).map(norm).filter(Boolean));return arr(data.patients).filter(p=>{const own=namesOf(p);return[...wanted].some(n=>own.has(n))})}
function choosePrimary(list=[]){return[...list].sort((a,b)=>linkedCount(b.id)-linkedCount(a.id)||String(a.createdAt||'').localeCompare(String(b.createdAt||'')))[0]||null}
function performed(a){return Boolean(a&&a.status!=='Cancelada'&&a.attendanceStatus!=='Desmarcou'&&(a.status==='Realizada'||['Presente','Compareceu'].includes(a.attendanceStatus)))}
function selectAppointment(patientId,date){const all=arr(data.appointments).filter(a=>a?.patientId===patientId&&a?.date===date&&a?.status!=='Cancelada'&&a?.attendanceStatus!=='Desmarcou');if(all.length===1)return all[0];const done=all.filter(performed);if(done.length===1)return done[0];return[...all].sort((a,b)=>String(a.time||'').localeCompare(String(b.time||'')))[0]||null}
async function loadToken(){if(!runtime.key)return null;let raw=null;try{raw=JSON.parse(localStorage.getItem(TOKEN_STORAGE)||'null')}catch{}if(!raw)return null;try{const t=await decryptJson(runtime.key,raw,'google-calendar-token');const scopes=String(t?.scope||'').split(/\s+/);if(t?.token&&Number(t.expiresAt)>Date.now()+60000&&scopes.includes(DRIVE_SCOPE))return t.token}catch{}return null}
async function gfetch(url,token){const r=await fetch(url,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok)throw new Error(`Google Drive respondeu ${r.status}.`);return r}
async function latestPatch(token){const url=new URL('https://www.googleapis.com/drive/v3/files');url.searchParams.set('q',`mimeType='application/vnd.google-apps.document' and trashed=false and name contains '${PATCH_PREFIX}'`);url.searchParams.set('orderBy','modifiedTime desc');url.searchParams.set('pageSize','10');url.searchParams.set('fields','files(id,name,modifiedTime,webViewLink)');const json=await(await gfetch(url,token)).json();return arr(json.files)[0]||null}
async function readPatch(file,token){const url=`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(file.id)}/export?mimeType=${encodeURIComponent('text/plain')}`;const text=await(await gfetch(url,token)).text(),start=text.indexOf('{'),end=text.lastIndexOf('}');if(start<0||end<=start)throw new Error('Patch clínico privado sem JSON válido.');const patch=JSON.parse(text.slice(start,end+1));if(patch?.schema!=='rm-clinical-reconciliation-patch-v1')throw new Error('Patch clínico privado incompatível.');return patch}
async function mergeExplicitDuplicates(spec){const list=candidates(spec?.names);if(!spec?.mergeDuplicates||list.length<2)return choosePrimary(list);const primary=choosePrimary(list);if(!primary)return null;for(const secondary of list.filter(p=>p.id!==primary.id)){if(conflict(primary,secondary))continue;const batches=[];for(const store of LINK_STORES){const changed=[];for(const row of arr(data[store]))if(row?.patientId===secondary.id){row.patientId=primary.id;row.updatedAt=nowISO();changed.push({...row})}if(changed.length)batches.push({storeName:store,values:changed})}for(const[k,v]of Object.entries(secondary)){if(['id','createdAt','updatedAt'].includes(k))continue;if((primary[k]===undefined||primary[k]===null||primary[k]===''||(Array.isArray(primary[k])&&!primary[k].length))&&v!==undefined&&v!==null&&v!=='')primary[k]=v}primary.updatedAt=nowISO();batches.push({storeName:'patients',values:[{...primary}]});await bulkPutEncryptedAtomic(batches,runtime.key,{verify:true});await deleteRecord('patients',secondary.id);data.patients=arr(data.patients).filter(p=>p?.id!==secondary.id);if(runtime.selectedPatientId===secondary.id)runtime.selectedPatientId=primary.id}return primary}
async function loadCatalog(){let raw=null;try{raw=JSON.parse(localStorage.getItem(CATALOG_STORAGE)||'null')}catch{}if(!raw)return null;try{return await decryptJson(runtime.key,raw,CATALOG_AAD)}catch{return null}}
async function saveCatalog(catalog){const enc=await encryptJson(runtime.key,catalog,CATALOG_AAD);localStorage.setItem(CATALOG_STORAGE,JSON.stringify(enc))}
function catalogItems(catalog){if(Array.isArray(catalog?.items))return catalog.items;if(catalog?.items&&typeof catalog.items==='object')return Object.values(catalog.items);return[]}
function scoreItemToAppointment(item,a){const t=minuteOf(item?.rawTime||item?.meetingTime),s=minuteOf(a?.time);if(t===null||s===null)return Infinity;const d=Math.max(30,Math.min(Number(a.duration)||50,120));return Math.min(Math.abs(t-s),Math.abs(t-(s+d)))}
function stablePaymentId(patient,receipt,index){const cents=Math.round(Number(receipt.value||0)*100);return`pay_private_${String(patient.id).replace(/[^a-zA-Z0-9_-]/g,'_')}_${String(receipt.date||'').replace(/-/g,'')}_${cents}_${index}`}
function findExistingReceipt(patient,receipt){return arr(data.payments).find(p=>p?.patientId===patient.id&&p?.status==='Pago'&&p?.date===receipt.date&&Math.abs(Number(p.value||0)-Number(receipt.value||0))<0.005)||null}
async function applyPatch(patch){const appointmentWrites=new Map(),patientWrites=new Map(),paymentWrites=new Map(),patientSpecs=[];let paid=0,status=0,merged=0,receipts=0,profiles=0;
  for(const spec of arr(patch?.patients)){
    const before=candidates(spec.names),patient=await mergeExplicitDuplicates(spec);if(before.length>1&&patient&&candidates(spec.names).length<before.length)merged+=before.length-candidates(spec.names).length;if(!patient)continue;patientSpecs.push({spec,patient});
    if(spec.profile&&typeof spec.profile==='object'){const next={...patient,...spec.profile,updatedAt:nowISO()};if(arr(spec.profile.payerAliases).length)next.payerAliases=[...new Set([...(arr(patient.payerAliases)),...spec.profile.payerAliases])];Object.assign(patient,next);patientWrites.set(patient.id,{...next});profiles++}
    for(const s of arr(spec.sessions)){const a=selectAppointment(patient.id,s.date);if(!a)continue;let next={...a};if(s.performed!==false&&(next.status!=='Realizada'||!['Presente','Compareceu'].includes(next.attendanceStatus))){next.status='Realizada';next.attendanceStatus='Presente'}if(s.paid===true){next.paymentReconciled=true;next.paymentReconciledAt=nowISO();next.paymentReconciliationSource='private-drive-audit-v360';paid++}if(s.recordExpected===true){next.geminiExpected=true;next.geminiExpectedAt=nowISO()}next.updatedAt=nowISO();appointmentWrites.set(next.id,next)}
    for(const[ri,receipt]of arr(spec.receipts).entries()){let row=findExistingReceipt(patient,receipt);const ids=arr(receipt.sessionDates).map(date=>selectAppointment(patient.id,date)?.id).filter(Boolean);if(row){row={...row};if(ids.length){row.appointmentIds=[...new Set([...(arr(row.appointmentIds)),...ids])];if(ids.length===1)row.appointmentId=ids[0]}if(receipt.competence&&!row.competence)row.competence=receipt.competence;if(receipt.sessionUnits&&!row.sessionUnits)row.sessionUnits=receipt.sessionUnits;row.updatedAt=nowISO()}else{row={id:stablePaymentId(patient,receipt,ri),patientId:patient.id,date:receipt.date,value:Number(receipt.value||0),status:'Pago',method:receipt.method||'PIX',competence:receipt.competence||String(receipt.date||'').slice(0,7),sessionUnits:Number(receipt.sessionUnits||0)||undefined,appointmentIds:ids,appointmentId:ids.length===1?ids[0]:'',note:receipt.note||'Recebimento conciliado a partir de documentação financeira privada.',source:{type:'private-drive-audit-v360'},createdAt:receipt.createdAt||nowISO(),updatedAt:nowISO()}}paymentWrites.set(row.id,row);receipts++}
  }
  for(const update of arr(patch?.statusUpdates)){const patient=choosePrimary(candidates(update.names));if(!patient||!update.status)continue;const next={...patient,status:String(update.status),updatedAt:nowISO()};if(['Inativo','Encerrado','Arquivado'].includes(next.status)){next.inactiveAt=update.effectiveDate||next.inactiveAt||nowISO().slice(0,10);next.careEndedAt=next.careEndedAt||next.inactiveAt}Object.assign(patient,next);patientWrites.set(patient.id,{...next});status++}
  const batches=[];if(appointmentWrites.size)batches.push({storeName:'appointments',values:[...appointmentWrites.values()]});if(patientWrites.size)batches.push({storeName:'patients',values:[...patientWrites.values()]});if(paymentWrites.size)batches.push({storeName:'payments',values:[...paymentWrites.values()]});if(batches.length)await bulkPutEncryptedAtomic(batches,runtime.key,{verify:true});
  for(const next of appointmentWrites.values()){const i=arr(data.appointments).findIndex(x=>x?.id===next.id);if(i>=0)data.appointments[i]=next}
  for(const next of patientWrites.values()){const i=arr(data.patients).findIndex(x=>x?.id===next.id);if(i>=0)data.patients[i]=next}
  for(const next of paymentWrites.values()){const i=arr(data.payments).findIndex(x=>x?.id===next.id);if(i>=0)data.payments[i]=next;else data.payments.push(next)}
  const catalog=await loadCatalog();let hints=0;if(catalog){catalog.manualLinks={...(catalog.manualLinks||{})};const items=catalogItems(catalog);for(const item of items.filter(x=>x?.status==='review'&&x?.fileId&&x?.meetingDate)){const possibilities=[];for(const{spec,patient}of patientSpecs){if(!arr(spec.sessions).some(s=>s.date===item.meetingDate&&s.recordExpected===true))continue;const a=selectAppointment(patient.id,item.meetingDate);if(!a)continue;possibilities.push({patient,a,score:scoreItemToAppointment(item,a)})}possibilities.sort((x,y)=>x.score-y.score);const best=possibilities[0],second=possibilities[1];if(!best||best.score>30||(second&&second.score-best.score<12))continue;catalog.manualLinks[item.fileId]=best.patient.id;item.reason=`Associação privada de alta confiança: sessão ${best.a.time||'—'}, diferença ${best.score} min.`;hints++}if(hints){catalog.updatedAt=nowISO();await saveCatalog(catalog)}}
  document.dispatchEvent(new CustomEvent('rm:local-data-changed',{detail:{kind:'private-clinical-reconciliation-v360',paid,status,merged,hints,receipts,profiles,at:nowISO()}}));window.__rmRender?.();return{paid,status,merged,hints,receipts,profiles}
}
async function run(){if(running||runtime.locked||!runtime.key||!runtime.dataReady)return null;running=true;try{const token=await loadToken();if(!token)return null;const file=await latestPatch(token);if(!file)return null;const marker=`${file.id}:${file.modifiedTime||''}`;if(localStorage.getItem(APPLIED_KEY)===marker)return{alreadyApplied:true};const patch=await readPatch(file,token),result=await applyPatch(patch);localStorage.setItem(APPLIED_KEY,marker);globalThis.__rmPrivateClinicalReconciliationStatus={version:VERSION,fileId:file.id,fileName:file.name,...result,at:nowISO()};setTimeout(()=>location.reload(),900);return globalThis.__rmPrivateClinicalReconciliationStatus}catch(error){console.warn('Conciliação clínica privada:',error);globalThis.__rmPrivateClinicalReconciliationStatus={version:VERSION,error:String(error?.message||error),at:nowISO()};return null}finally{running=false}}
document.addEventListener('rm:data-ready',()=>setTimeout(()=>void run(),700));document.addEventListener('rm:google-workspace-authorized',()=>setTimeout(()=>void run(),500));globalThis.__rmPrivateClinicalReconciliation={version:VERSION,run,status:()=>globalThis.__rmPrivateClinicalReconciliationStatus||null};
