import {data,runtime,preferences,selectedPatient} from './state.js';
import {encryptJson,decryptJson} from './crypto.js';
import {bulkPutEncrypted} from './database.js';
import {toast,esc,modal,closeModal} from './ui.js';

const VERSION='2.3.0';
const TOKEN_STORAGE='rm.google.calendar.token.v1';
const CATALOG_STORAGE='rm.google.clinical.reconcile.v230';
const CATALOG_AAD='google-clinical-reconcile-v230';
const DRIVE_SCOPE='https://www.googleapis.com/auth/drive.readonly';
const BACKFILL_DATE='2026-05-01';
const BACKFILL_ISO='2026-05-01T03:00:00Z';
const POLL_MS=5*60*1000;
const PROMPT_VERSION='TCC-CFP-v2.3';
const PROFESSIONAL_PROMPT=`Atue como apoio à documentação clínica de um psicólogo com referencial principal em Terapia Cognitivo-Comportamental. A partir exclusivamente do resumo e das anotações produzidas pelo Google Meet/Gemini, redija uma evolução de prontuário sintética, objetiva, cronológica e tecnicamente defensável. Registre somente elementos necessários ao acompanhamento: foco/demanda da sessão, evolução clínica observável, procedimentos técnico-científicos efetivamente descritos, resposta do paciente, decisões clínicas, tarefas e encaminhamentos. Não invente diagnóstico, hipótese, técnica, risco, sintoma, fala, resultado ou conduta ausente na fonte. Não copie a transcrição integral. Evite juízos morais, detalhes íntimos sem pertinência clínica e linguagem estigmatizante. Preserve a distinção entre relato do paciente e intervenção profissional. O texto final deve ser revisado pelo psicólogo antes da finalização.`;

let token='';
let expiresAt=0;
let tokenScope='';
let syncState='unknown';
let scanning=false;
let timer=null;
let lastRunAt=0;
let catalog={version:1,lastScanAt:'',lastError:'',filesSeen:0,items:[]};

const arr=v=>Array.isArray(v)?v.filter(Boolean):[];
const normalize=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const pad=n=>String(n).padStart(2,'0');
const activePatients=()=>arr(data.patients).filter(p=>!['Encerrado','Arquivado'].includes(p?.status||'')&&normalize(p?.name)!==normalize(preferences.professionalName));
const patientById=id=>arr(data.patients).find(p=>p?.id===id)||null;
const recordByFileId=id=>arr(data.records).find(r=>r?.source?.fileId===id||r?.id===`gmrec_${id}`)||null;
const appointmentByFileId=id=>arr(data.appointments).find(a=>a?.source?.fileId===id||a?.gemini?.fileId===id||a?.id===`gmappt_${id}`)||null;
const tokenValid=()=>Boolean(token&&expiresAt>Date.now()+60000);
const driveGranted=()=>tokenScope.split(/\s+/).includes(DRIVE_SCOPE);

function meetingIso(date,time){return `${date}T${time}:00-03:00`}
function addDays(date,days){const d=new Date(`${date}T12:00:00-03:00`);d.setDate(d.getDate()+days);return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`}
function normalizeAgendaSlot(date,time){let [h,m]=String(time||'00:00').split(':').map(Number);if(!Number.isFinite(h)||!Number.isFinite(m))return{date,time};if(m>=30){h+=1;if(h>=24){h=0;date=addDays(date,1)}}return{date,time:`${pad(h)}:00`,rawTime:time}}
function meetingInfo(file){const title=String(file?.name||'');let m=title.match(/(\d{4})[\/-](\d{2})[\/-](\d{2})\s+(\d{2}):(\d{2})/);if(m)return normalizeAgendaSlot(`${m[1]}-${m[2]}-${m[3]}`,`${m[4]}:${m[5]}`);m=title.match(/(\d{2})[\/-](\d{2})[\/-](\d{4})\s+(\d{2}):(\d{2})/);if(m)return normalizeAgendaSlot(`${m[3]}-${m[2]}-${m[1]}`,`${m[4]}:${m[5]}`);return null}
function sourceStamp(file,meeting){return file?.modifiedTime||file?.createdTime||meetingIso(meeting.date,meeting.time)}
function emit(status,detail=''){document.dispatchEvent(new CustomEvent('rm:gemini-status',{detail:{status,detail,lastScanAt:catalog.lastScanAt,lastError:catalog.lastError}}))}
function mergeData(store,values){const list=data[store]||(data[store]=[]);for(const value of values){const i=list.findIndex(x=>x?.id===value.id);if(i>=0)list[i]=value;else list.push(value)}}

async function loadToken(){token='';expiresAt=0;tokenScope='';if(!runtime.key)return false;let raw=null;try{raw=JSON.parse(localStorage.getItem(TOKEN_STORAGE)||'null')}catch{}if(!raw)return false;try{const v=await decryptJson(runtime.key,raw,'google-calendar-token');if(v?.token&&Number(v.expiresAt)>Date.now()+60000){token=v.token;expiresAt=Number(v.expiresAt);tokenScope=String(v.scope||'');return true}}catch{}return false}
async function loadCatalog(){catalog={version:1,lastScanAt:'',lastError:'',filesSeen:0,items:[]};if(!runtime.key)return;let raw=null;try{raw=JSON.parse(localStorage.getItem(CATALOG_STORAGE)||'null')}catch{}if(!raw)return;try{const v=await decryptJson(runtime.key,raw,CATALOG_AAD);if(v&&Array.isArray(v.items))catalog={version:1,lastScanAt:v.lastScanAt||'',lastError:v.lastError||'',filesSeen:Number(v.filesSeen||0),items:v.items.slice(-600)}}catch{}}
async function saveCatalog(){if(!runtime.key)return;const encrypted=await encryptJson(runtime.key,{...catalog,version:1,items:arr(catalog.items).slice(-600)},CATALOG_AAD);localStorage.setItem(CATALOG_STORAGE,JSON.stringify(encrypted))}
function upsertCatalog(item){const i=arr(catalog.items).findIndex(x=>x?.fileId===item.fileId);if(i>=0)catalog.items[i]={...catalog.items[i],...item};else catalog.items.push(item)}

async function gfetch(url,options={}){if(!tokenValid())throw new Error('Renove a conexão do Google Workspace.');const r=await fetch(url,{...options,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json',...(options.headers||{})}});if(r.status===401)throw new Error('A autorização do Google Workspace expirou.');if(r.status===403)throw new Error('A conexão Google não possui permissão suficiente para ler o Drive.');return r}
async function listDocs(){const out=[];let pageToken='';do{const u=new URL('https://www.googleapis.com/drive/v3/files');u.searchParams.set('q',`mimeType='application/vnd.google-apps.document' and trashed=false and createdTime >= '${BACKFILL_ISO}' and name contains 'Gemini'`);u.searchParams.set('pageSize','1000');u.searchParams.set('orderBy','createdTime asc');u.searchParams.set('fields','nextPageToken,files(id,name,createdTime,modifiedTime,webViewLink,mimeType)');if(pageToken)u.searchParams.set('pageToken',pageToken);const r=await gfetch(u);if(!r.ok)throw new Error(`Google Drive respondeu ${r.status}.`);const j=await r.json();out.push(...arr(j.files));pageToken=j.nextPageToken||''}while(pageToken);return out.filter(f=>/anota[cç][oõ]es.*gemini|gemini.*anota[cç][oõ]es|notes.*gemini|gemini.*notes/i.test(f?.name||''))}
async function exportDoc(id){const r=await gfetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(id)}/export?mimeType=${encodeURIComponent('text/plain')}`);if(!r.ok)throw new Error(`Não foi possível ler a anotação ${id}.`);return r.text()}

function participantLabels(raw=''){const found=new Set();for(const m of String(raw).matchAll(/^\s*([^\n:]{2,90}):\s+/gm)){const n=normalize(m[1]);if(n)found.add(n)}for(const m of String(raw).matchAll(/\[([^\]\n]{2,90})\]/g)){const n=normalize(m[1]);if(n)found.add(n)}return [...found]}
function patientAliases(p){return [...new Set([p?.name,p?.preferredName].map(normalize).filter(Boolean))]}
function firstName(p){return normalize(String(p?.preferredName||p?.name||'').trim().split(/\s+/)[0])}
function uniqueFirstPatient(name,patients){const key=normalize(name).split(' ')[0];if(key.length<4)return null;const hits=patients.filter(p=>firstName(p)===key);return hits.length===1?hits[0]:null}
function buildLearnedAliases(docs,patients){const map=new Map();for(const {raw} of docs){for(const m of String(raw).matchAll(/([^\n:()]{2,80})\s*\(([^)\n]{2,60})\)/g)){const outer=normalize(m[1]),inner=normalize(m[2]);if(!outer||!inner)continue;const p=patients.find(x=>patientAliases(x).some(a=>a===inner||a.startsWith(inner+' ')||inner.startsWith(a+' ')))||uniqueFirstPatient(inner,patients);if(p)map.set(outer,p.id)}}return map}
function identifyPatient(raw,learned){const labels=participantLabels(raw).filter(l=>l!==normalize(preferences.professionalName));const patients=activePatients(),rank=[];for(const p of patients){let score=0,evidence='';for(const alias of patientAliases(p)){if(alias.split(' ').length>=2&&labels.some(l=>l===alias||l.startsWith(alias+' ')||alias.startsWith(l+' '))){score=180;evidence='nome completo no Gemini';break}}if(!score){for(const l of labels){if(learned.get(l)===p.id){score=170;evidence='apelido/identificador aprendido no Gemini';break}}}if(!score){const first=firstName(p);if(first.length>=4&&patients.filter(x=>firstName(x)===first).length===1&&labels.some(l=>l===first||l.startsWith(first+' '))){score=135;evidence='primeiro nome único no cofre'}}if(score)rank.push({patient:p,score,evidence})}rank.sort((a,b)=>b.score-a.score);if(!rank.length)return{patient:null,reason:'Paciente não identificado com segurança.'};if(rank.length>1&&rank[0].score-rank[1].score<20)return{patient:null,reason:'Associação ambígua entre pacientes.'};return rank[0]}

function section(raw,labels,endLabels=[]){const lines=String(raw||'').replace(/^\uFEFF/,'').split(/\r?\n/),starts=labels.map(normalize),ends=endLabels.map(normalize),start=lines.findIndex(x=>starts.includes(normalize(x)));if(start<0)return'';let end=lines.length;for(let i=start+1;i<lines.length;i++){if(ends.includes(normalize(lines[i]))){end=i;break}}return lines.slice(start+1,end).join('\n').replace(/\n{3,}/g,'\n\n').trim()}
function cleanLine(v=''){return String(v).replace(/\s*\(\d{2}:\d{2}:\d{2}\)(?:\s*\(\d{2}:\d{2}:\d{2}\))*\s*/g,' ').replace(/^[-*•]\s*/,'').replace(/\s+/g,' ').trim()}
function escapeRx(v=''){return String(v).replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}
function neutralize(v,patient){let out=String(v||'');for(const name of [patient?.name,patient?.preferredName].filter(Boolean).sort((a,b)=>b.length-a.length))out=out.replace(new RegExp(`\\b${escapeRx(name)}\\b`,'gi'),'Paciente');out=out.replace(new RegExp(`\\b${escapeRx(preferences.professionalName)}\\b`,'gi'),'Profissional');return out}
function buildClinicalDraft(raw,patient){const source=String(raw).split(/📖\s*Transcri[cç][aã]o/i)[0].trim();const summary=neutralize(section(source,['Resumo','Summary'],['Próximas etapas','Proximas etapas','Next steps','Detalhes','Details']),patient);const details=section(source,['Detalhes','Details'],['Revise as anotações','Revise as anotacoes']);const lines=String(details).split(/\r?\n/).map(cleanLine).filter(Boolean);const procedureRx=/orient|psicoeduc|reestrutur|question|socr[aá]t|registro|exposi[cç]|ativa[cç][aã]o comport|planej|trein|habilidade|relax|respira|mindful|preven[cç][aã]o|discut|trabalh|interven|explor|identific|valida[cç]|modelo|matriz|t[eé]cnica/i;const responseRx=/relat|referiu|percebeu|reconheceu|identificou|avaliou|decidiu|concordou|demonstrou|apresentou|melhora|dificuldade|redu[cç][aã]o|aumento|mant[eé]m|descreveu/i;const procedures=lines.filter(x=>procedureRx.test(x)).slice(0,6).map(x=>`• ${neutralize(x,patient)}`).join('\n');const responses=lines.filter(x=>responseRx.test(x)&&!procedureRx.test(x)).slice(0,5).map(x=>`• ${neutralize(x,patient)}`).join('\n');const steps=section(source,['Próximas etapas','Proximas etapas','Next steps'],['Detalhes','Details']).split(/\r?\n/).map(cleanLine).filter(Boolean).map(x=>`• ${neutralize(x,patient)}`).join('\n');const fallback=neutralize(source.slice(0,3500),patient);return{title:'Evolução de sessão',text:[`Foco clínico e evolução da sessão\n${summary||fallback||'Síntese automática indisponível.'}`,procedures?`Procedimentos técnico-científicos adotados\n${procedures}`:'',responses?`Resposta do paciente e resultados observados\n${responses}`:'',steps?`Plano terapêutico, tarefas e encaminhamentos\n${steps}`:''].filter(Boolean).join('\n\n'),followup:steps.replace(/^•\s*/gm,'').trim()}}
function deriveDuration(raw=''){let max=0;for(const m of String(raw).matchAll(/\b(\d{2}):(\d{2}):(\d{2})\b/g)){const sec=Number(m[1])*3600+Number(m[2])*60+Number(m[3]);if(Number.isFinite(sec))max=Math.max(max,sec)}const min=Math.ceil(max/60);return min>=20&&min<=180?min:Number(preferences.defaultSessionMinutes||50)}
function findAppointment(patientId,meeting,fileId){const direct=appointmentByFileId(fileId);if(direct)return direct;const target=new Date(meetingIso(meeting.date,meeting.time)).getTime();const exact=arr(data.appointments).filter(a=>a?.patientId===patientId&&a?.date===meeting.date&&a?.status!=='Cancelada').map(a=>({a,d:Math.abs(new Date(meetingIso(a.date,a.time||'00:00')).getTime()-target)})).filter(x=>x.d<=90*60000).sort((x,y)=>x.d-y.d);if(exact.length)return exact[0].a;const movable=arr(data.appointments).filter(a=>a?.patientId===patientId&&a?.status!=='Cancelada'&&!arr(data.records).some(r=>r?.appointmentId===a.id)).map(a=>({a,d:Math.abs(new Date(meetingIso(a.date,a.time||'00:00')).getTime()-target)})).filter(x=>x.d<=96*3600000).sort((x,y)=>x.d-y.d);if(movable.length===1)return movable[0].a;if(movable.length>1&&movable[1].d-movable[0].d>18*3600000)return movable[0].a;return null}

async function materialize(entry,{forceRegenerate=false}={}){if(syncState!=='synced')throw new Error('Aguarde Dados ✓ antes de criar prontuários.');const patient=patientById(entry.patientId);if(!patient)throw new Error('Paciente não localizado.');await loadToken();const raw=await exportDoc(entry.fileId),draft=buildClinicalDraft(raw,patient),stamp=entry.modifiedTime||meetingIso(entry.meetingDate,entry.meetingTime),created=meetingIso(entry.meetingDate,entry.meetingTime);let appointment=findAppointment(patient.id,{date:entry.meetingDate,time:entry.meetingTime},entry.fileId);if(appointment&&(appointment.status==='Cancelada'||appointment.attendanceStatus==='Desmarcou'))appointment=null;if(appointment){appointment={...appointment,date:entry.meetingDate,time:entry.meetingTime,duration:appointment.duration||deriveDuration(raw),status:'Realizada',attendanceStatus:'Presente',updatedAt:stamp,gemini:{fileId:entry.fileId,url:entry.url||'',meetingDate:entry.meetingDate,meetingTime:entry.meetingTime,rawTime:entry.rawTime||entry.meetingTime}}}else appointment={id:`gmappt_${entry.fileId}`,patientId:patient.id,date:entry.meetingDate,time:entry.meetingTime,duration:deriveDuration(raw),status:'Realizada',attendanceStatus:'Presente',modality:'On-line',recurrence:'Avulso',note:'Sessão confirmada pelas Anotações do Google Meet/Gemini.',createdAt:created,updatedAt:stamp,source:{type:'google-meet-gemini',fileId:entry.fileId,url:entry.url||''}};let record=recordByFileId(entry.fileId);const manualSameDay=arr(data.records).find(r=>r?.patientId===patient.id&&r?.date===entry.meetingDate&&!r?.source?.fileId);if(!record&&manualSameDay){record=manualSameDay;entry.status='covered';entry.recordId=record.id}else if(record?.status==='Finalizado'&&!forceRegenerate){entry.status='materialized';entry.recordId=record.id}else{record={...(record||{}),id:record?.id||`gmrec_${entry.fileId}`,patientId:patient.id,appointmentId:appointment.id,title:draft.title,date:entry.meetingDate,status:'Rascunho IA',text:draft.text,followup:draft.followup,requiresReview:true,createdAt:record?.createdAt||created,updatedAt:stamp,addenda:record?.addenda||[],source:{...(record?.source||{}),type:'google-meet-gemini',fileId:entry.fileId,url:entry.url||'',title:entry.name||'',modifiedTime:entry.modifiedTime||'',version:VERSION,clinicalPromptVersion:PROMPT_VERSION,aiSource:'Google Meet/Gemini'}};entry.status='materialized';entry.recordId=record.id}
  const writes=[bulkPutEncrypted('appointments',[appointment],runtime.key)];if(record&&!manualSameDay)writes.push(bulkPutEncrypted('records',[record],runtime.key));const previous=globalThis.__rmSyncApplying;globalThis.__rmSyncApplying=true;try{await Promise.all(writes)}finally{globalThis.__rmSyncApplying=previous}mergeData('appointments',[appointment]);if(record&&!manualSameDay)mergeData('records',[record]);entry.appointmentId=appointment.id;entry.updatedAt=stamp;await saveCatalog();if(record&&!manualSameDay)document.dispatchEvent(new CustomEvent('rm:gemini-record-created',{detail:{recordId:record.id,patientId:patient.id}}));return{appointment,record}}

async function reconcile({quiet=true,force=false}={}){if(scanning||runtime.locked||!runtime.key||!runtime.dataReady||globalThis.__rmSyncApplying)return;if(syncState!=='synced'){emit('paused',syncState==='conflict'?'Gemini pausado: resolva o conflito de Dados.':'Aguardando Dados ✓.');return}if(!force&&Date.now()-lastRunAt<30000)return;await loadToken();if(!tokenValid()||!driveGranted()){emit('disconnected','Renove o Google Workspace para Agenda + Drive.');return}scanning=true;lastRunAt=Date.now();emit('scanning','Conciliando sessões do Meet/Gemini.');try{const files=await listDocs();catalog.filesSeen=files.length;const docs=[];for(const file of files){const meeting=meetingInfo(file);if(!meeting)continue;const previous=arr(catalog.items).find(x=>x.fileId===file.id);if(previous?.modifiedTime===file.modifiedTime&&['materialized','covered','review'].includes(previous.status)&&recordByFileId(file.id)){docs.push({file,meeting,raw:null,previous});continue}const raw=await exportDoc(file.id);docs.push({file,meeting,raw,previous})}const rawDocs=docs.filter(x=>x.raw),learned=buildLearnedAliases(rawDocs,activePatients());let changed=false;for(const d of docs){if(!d.raw){upsertCatalog({...d.previous,fileId:d.file.id,name:d.file.name,url:d.file.webViewLink||'',modifiedTime:d.file.modifiedTime||'',meetingDate:d.meeting.date,meetingTime:d.meeting.time,rawTime:d.meeting.rawTime||d.meeting.time});continue}const match=identifyPatient(d.raw,learned);if(!match.patient){upsertCatalog({fileId:d.file.id,name:d.file.name,url:d.file.webViewLink||'',modifiedTime:d.file.modifiedTime||'',meetingDate:d.meeting.date,meetingTime:d.meeting.time,rawTime:d.meeting.rawTime||d.meeting.time,status:'review',reason:match.reason,version:VERSION});continue}const entry={fileId:d.file.id,name:d.file.name,url:d.file.webViewLink||`https://docs.google.com/document/d/${d.file.id}`,modifiedTime:d.file.modifiedTime||sourceStamp(d.file,d.meeting),patientId:match.patient.id,meetingDate:d.meeting.date,meetingTime:d.meeting.time,rawTime:d.meeting.rawTime||d.meeting.time,status:'ready',reason:match.evidence,version:VERSION};upsertCatalog(entry);const existing=recordByFileId(entry.fileId);if(existing){entry.status='materialized';entry.recordId=existing.id;continue}await materialize(entry);changed=true}catalog.lastScanAt=new Date().toISOString();catalog.lastError='';await saveCatalog();if(changed){document.dispatchEvent(new CustomEvent('rm:local-data-changed',{detail:{storeName:'records',kind:'gemini-reconcile',at:catalog.lastScanAt,source:'gemini-v230'}}));window.__rmRender?.()}emit('connected',`${catalog.filesSeen} arquivo(s) conferido(s).`);decorateAll();if(!quiet)toast('Gemini conciliado com Agenda e Prontuário.','success')}catch(err){catalog.lastError=String(err?.message||err);await saveCatalog().catch(()=>{});emit('error',catalog.lastError);if(!quiet)toast(catalog.lastError,'error')}finally{scanning=false;decorateAll()}}

function stats(){const items=arr(catalog.items),safe=items.filter(x=>x.patientId),review=items.filter(x=>x.status==='review'),records=safe.filter(x=>recordByFileId(x.fileId));return{items,safe,review,records}}
function settingsMarkup(){const s=stats(),last=catalog.lastScanAt?new Date(catalog.lastScanAt).toLocaleString('pt-BR'):'Ainda não executado';return`<section id="rm-gemini-clinical" class="card mt-16"><div class="card-title"><div><div class="eyebrow">Integração clínica automática</div><h3 class="mb-0">Google Meet · Gemini</h3></div><span class="badge">${scanning?'Conferindo':syncState!=='synced'?'Pausado':tokenValid()&&driveGranted()?'Conectado':'Reconectar'}</span></div><p class="muted">As Anotações do Gemini são rastreadas desde <strong>01/05/2026</strong>. O nome do participante identifica o paciente; data e horário do Meet confirmam a sessão. Para a Agenda, minutos são normalizados para o horário cheio mais próximo (ex.: 20:04 → 20:00).</p><div class="gemini-metrics"><span><strong>${s.safe.length}</strong> sessões identificadas</span><span><strong>${s.records.length}</strong> prontuários</span><span><strong>${s.review.length}</strong> revisar</span><span><strong>${catalog.filesSeen}</strong> arquivos Gemini</span></div><div class="small muted mt-12">Última conciliação: ${esc(last)}.</div><div class="flex gap-8 wrap mt-16"><button class="btn" data-action="gemini-reconcile-now" ${syncState==='synced'?'':'disabled'}>Conciliar agora</button><button class="btn secondary" data-action="workspace-connect">Renovar Google Workspace</button></div><div class="notice mt-12"><strong>Prontuário:</strong> cada sessão associada com segurança gera um Rascunho IA TCC. A transcrição integral permanece no Drive e o registro final exige revisão profissional.</div></section>`}
function decorateSettings(){if(runtime.locked||runtime.route!=='settings')return;const main=document.getElementById('main-content');if(!main)return;const old=document.getElementById('rm-gemini-clinical'),wrap=document.createElement('div');wrap.innerHTML=settingsMarkup();if(old)old.replaceWith(wrap.firstElementChild);else main.appendChild(wrap.firstElementChild)}
function enforcePatientFocus(){if(runtime.locked||runtime.route!=='patients')return;const p=selectedPatient(),main=document.getElementById('main-content');if(!main||!p)return;main.querySelector('.toolbar')?.remove();main.querySelector('#patient-grid')?.remove();const workspace=main.querySelector('#patient-workspace');if(workspace)workspace.classList.remove('mt-24');const actions=main.querySelector('.patient-context .flex');if(actions&&!actions.querySelector('[data-tab="records"]')){const b=document.createElement('button');b.className='btn secondary';b.dataset.action='patient-tab';b.dataset.tab='records';b.textContent='Prontuário';actions.prepend(b)}if(actions&&!actions.querySelector('[data-action="gemini-ai-hub"]')){const b=document.createElement('button');b.className='btn';b.dataset.action='gemini-ai-hub';b.textContent='Gerar prontuário por IA';actions.prepend(b)}}
function patientItems(){const p=selectedPatient();return p?arr(catalog.items).filter(x=>x.patientId===p.id).sort((a,b)=>`${b.meetingDate} ${b.meetingTime}`.localeCompare(`${a.meetingDate} ${a.meetingTime}`)):[]}
function decorateRecords(){if(runtime.locked||runtime.route!=='patients'||runtime.patientTab!=='records')return;const p=selectedPatient();if(!p)return;const main=document.getElementById('main-content');if(!main)return;let bar=document.getElementById('rm-ai-record-toolbar');if(!bar){bar=document.createElement('div');bar.id='rm-ai-record-toolbar';bar.className='card mb-16';const grid=main.querySelector('.grid');if(grid)grid.insertAdjacentElement('beforebegin',bar)}const items=patientItems(),missing=items.filter(x=>!recordByFileId(x.fileId));bar.innerHTML=`<div class="card-title"><div><div class="eyebrow">Prontuário assistido por IA</div><h3 class="mb-0">${items.length} sessão(ões) do Gemini</h3></div><span class="badge">${missing.length?`${missing.length} pendente(s)`:'Atualizado'}</span></div><p class="small muted">O botão aplica o prompt clínico ${PROMPT_VERSION} ao resumo produzido pelo Gemini e gera um rascunho TCC para revisão. Nenhuma transcrição integral é copiada.</p><div class="flex gap-8 wrap"><button class="btn" data-action="gemini-ai-hub">Gerar prontuário por IA</button><button class="btn secondary" data-action="gemini-reconcile-now">Conciliar sessões</button></div>`}
function aiHub(){const p=selectedPatient();if(!p)return;const items=patientItems();modal('Gerar prontuário por IA',items.length?`<div class="notice"><strong>Prompt clínico ${PROMPT_VERSION}</strong><br>O conteúdo usa somente as Anotações do Gemini e permanece sujeito à revisão profissional.</div><div class="gemini-queue mt-16">${items.map(item=>{const r=recordByFileId(item.fileId);return`<div class="gemini-queue-row"><div><strong>${esc(item.meetingDate?.split('-').reverse().join('/')||'—')} · ${esc(item.meetingTime||'')}</strong><div class="tiny muted">${r?esc(r.status||'Prontuário existente'):'Prontuário pendente'}</div></div><button class="btn ${r?'secondary':''}" data-action="gemini-ai-generate" data-id="${esc(item.fileId)}">${r?'Regenerar rascunho IA':'Gerar prontuário por IA'}</button></div>`}).join('')}</div>`:'<div class="notice">Nenhuma sessão Gemini associada a este paciente. Use Conciliar agora.</div>',`<button class="btn secondary" data-action="close-modal">Fechar</button>`,true)}
async function regenerate(fileId){const item=arr(catalog.items).find(x=>x.fileId===fileId);if(!item)throw new Error('Sessão Gemini não localizada.');const existing=recordByFileId(fileId);if(existing?.status==='Finalizado')throw new Error('Este prontuário está finalizado e não pode ser sobrescrito. Use adendo quando necessário.');await materialize(item,{forceRegenerate:true});closeModal();runtime.patientTab='records';window.__rmRender?.();toast('Rascunho IA gerado. Revise antes de finalizar.','success')}
function decorateAll(){decorateSettings();enforcePatientFocus();decorateRecords()}

async function activate(){await Promise.all([loadCatalog(),loadToken()]);emit(tokenValid()&&driveGranted()?(syncState==='synced'?'connected':'paused'):'disconnected',syncState==='synced'?'Google Meet/Gemini pronto.':'Aguardando Dados ✓.');decorateAll();clearInterval(timer);timer=setInterval(()=>{if(!document.hidden&&navigator.onLine&&syncState==='synced'&&Date.now()-lastRunAt>=POLL_MS)void reconcile({quiet:true})},POLL_MS)}

document.addEventListener('click',async e=>{const el=e.target.closest?.('[data-action]');if(!el)return;const action=el.dataset.action;if(action==='choose-patient'&&runtime.route==='patients'){e.preventDefault();e.stopImmediatePropagation();runtime.selectedPatientId='';runtime.patientTab='summary';window.__rmRender?.();return}if(!['gemini-reconcile-now','gemini-ai-hub','gemini-ai-generate'].includes(action))return;e.preventDefault();e.stopImmediatePropagation();try{if(action==='gemini-reconcile-now'){await reconcile({quiet:false,force:true});return}if(action==='gemini-ai-hub'){runtime.patientTab='records';window.__rmRender?.();setTimeout(aiHub,80);return}if(action==='gemini-ai-generate'){await regenerate(el.dataset.id);return}}catch(err){toast(err?.message||'Não foi possível concluir a ação.','error')}},true);
document.addEventListener('rm:data-ready',()=>void activate());
document.addEventListener('rm:rendered',()=>setTimeout(decorateAll,0));
document.addEventListener('rm:google-workspace-authorized',()=>setTimeout(()=>void activate(),100));
document.addEventListener('rm:sync-status',e=>{const previous=syncState;syncState=e.detail?.status||syncState;if(syncState==='conflict'){emit('paused','Gemini pausado: resolva o conflito de Dados.')}else if(syncState==='synced'&&previous!=='synced'){emit('connected','Dados sincronizados.');setTimeout(()=>void reconcile({quiet:true,force:true}),500)}else if(syncState!=='synced')emit('paused','Aguardando Dados ✓.');decorateAll()});
document.addEventListener('visibilitychange',()=>{if(!document.hidden&&navigator.onLine&&syncState==='synced'&&Date.now()-lastRunAt>=POLL_MS)void reconcile({quiet:true})});
window.addEventListener('online',()=>{if(syncState==='synced'&&Date.now()-lastRunAt>=POLL_MS)void reconcile({quiet:true})});
