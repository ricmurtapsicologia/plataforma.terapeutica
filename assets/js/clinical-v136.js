import {data,runtime,preferences,uid,nowISO,patientById,selectedPatient} from './state.js';
import {putEncrypted,deleteRecord} from './database.js';
import {validatePatient,sanitizeText,assert} from './validation.js';
import {openWhatsApp,openEmail,renderTemplate,DEFAULT_MESSAGES} from './communications.js';
import {closeModal,toast,fmtDate} from './ui.js';
import {renderAppointments as renderAppointments138} from './modules/appointments.js?v=1.3.8';

const VERSION='138';
const customActions=new Set(['save-patient','save-appointment','schedule-selected-patient','quick-schedule-patient','agenda-prev-week','agenda-next-week','agenda-today','appointment-whatsapp','patient-whatsapp','patient-email','document-email','clear-patient-local-data','delete-appointment','delete-appointment-series','register-session-from-agenda','clear-patient-draft']);
const q=s=>document.querySelector(s);
const arr=v=>Array.isArray(v)?v.filter(Boolean):[];
let draftTimer=null;

function rerender(route=runtime.route){runtime.route=route;location.hash=route;window.dispatchEvent(new Event('hashchange'))}
async function saveEntity(store,item){assert(runtime.key,'Sessão bloqueada. Entre novamente.');item.updatedAt=nowISO();const list=data[store]||(data[store]=[]),i=list.findIndex(x=>x?.id===item.id);if(i>=0)list[i]=item;else list.push(item);await putEncrypted(store,item,runtime.key);return item}
async function removeEntity(store,id){data[store]=arr(data[store]).filter(x=>x.id!==id);await deleteRecord(store,id)}
function firstName(p){return sanitizeText((p?.preferredName||p?.name||'').split(/\s+/)[0]||'')}
function escAttr(v=''){return String(v??'').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;')}
function maskPhone(input){let n=input.value.replace(/\D/g,'').slice(0,11);if(n.length>10)input.value=`(${n.slice(0,2)}) ${n.slice(2,7)}-${n.slice(7)}`;else if(n.length>6)input.value=`(${n.slice(0,2)}) ${n.slice(2,6)}-${n.slice(6)}`;else if(n.length>2)input.value=`(${n.slice(0,2)}) ${n.slice(2)}`;else input.value=n}
function maskCpf(input){let n=input.value.replace(/\D/g,'').slice(0,11);input.value=n.replace(/^(\d{3})(\d)/,'$1.$2').replace(/^(\d{3})\.(\d{3})(\d)/,'$1.$2.$3').replace(/\.(\d{3})(\d)/,'.$1-$2')}
function maskCep(input){let n=input.value.replace(/\D/g,'').slice(0,8);input.value=n.length>5?`${n.slice(0,5)}-${n.slice(5)}`:n}
function draftId(){return`patient_form_draft_${q('#p-id')?.value||'new'}`}
function getDraft(){return arr(data.settings).find(x=>x.id===draftId())||null}

function collectPatientForm(){return{id:draftId(),kind:'patientDraft',patientId:q('#p-id')?.value||'',name:q('#p-name')?.value||'',preferredName:q('#p-preferred')?.value||'',code:q('#p-code')?.value||'',status:q('#p-status')?.value||'Ativo',birthDate:q('#p-birth')?.value||'',modality:q('#p-modality')?.value||'On-line',phone:q('#p-phone')?.value||'',email:q('#p-email')?.value||'',channel:q('#p-channel')?.value||'WhatsApp',value:q('#p-value')?.value||'',cpf:q('#p-cpf')?.value||'',rg:q('#p-rg')?.value||'',sessionRecurrence:q('#p-session-recurrence')?.value||'Avulso',summary:q('#p-summary')?.value||'',photo:q('#p-photo-preview')?.dataset.photo||'',whatsappConsent:Boolean(q('#p-wa-consent')?.checked),emailConsent:Boolean(q('#p-email-consent')?.checked),address:{zip:q('#p-zip')?.value||'',street:q('#p-street')?.value||'',number:q('#p-number')?.value||'',complement:q('#p-complement')?.value||'',neighborhood:q('#p-neighborhood')?.value||'',city:q('#p-city')?.value||'',state:q('#p-state')?.value||''},updatedAt:nowISO()}}
async function savePatientDraft(){if(!q('#p-id')||!runtime.key)return;await saveEntity('settings',collectPatientForm())}
function queuePatientDraft(){clearTimeout(draftTimer);draftTimer=setTimeout(()=>savePatientDraft().catch(console.error),450)}
async function deletePatientDraft(){const d=getDraft();if(d)await removeEntity('settings',d.id)}
function setValue(id,value){const el=q(id);if(el&&value!==undefined&&value!==null)el.value=value}
function restoreDraft(){const d=getDraft();if(!d)return;setValue('#p-name',d.name);setValue('#p-preferred',d.preferredName);setValue('#p-code',d.code);setValue('#p-status',d.status);setValue('#p-birth',d.birthDate);setValue('#p-modality',d.modality);setValue('#p-phone',d.phone);setValue('#p-email',d.email);setValue('#p-channel',d.channel);setValue('#p-value',d.value);setValue('#p-cpf',d.cpf);setValue('#p-rg',d.rg);setValue('#p-session-recurrence',d.sessionRecurrence);setValue('#p-summary',d.summary);setValue('#p-zip',d.address?.zip);setValue('#p-street',d.address?.street);setValue('#p-number',d.address?.number);setValue('#p-complement',d.address?.complement);setValue('#p-neighborhood',d.address?.neighborhood);setValue('#p-city',d.address?.city);setValue('#p-state',d.address?.state);if(q('#p-wa-consent'))q('#p-wa-consent').checked=Boolean(d.whatsappConsent);if(q('#p-email-consent'))q('#p-email-consent').checked=Boolean(d.emailConsent);if(d.photo)applyPhotoPreview(d.photo);if(q('#p-phone'))maskPhone(q('#p-phone'));if(q('#p-cpf'))maskCpf(q('#p-cpf'));if(q('#p-zip'))maskCep(q('#p-zip'))}

function applyPhotoPreview(src){const box=q('#p-photo-preview');if(!box)return;box.dataset.photo=src||'';box.innerHTML=src?`<img src="${src}" alt="Prévia da foto do paciente"><button type="button" class="photo-remove" data-action="remove-patient-photo" title="Remover foto">×</button>`:`<div class="photo-placeholder">Sem foto</div>`}
function compressPhoto(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=reject;reader.onload=()=>{const img=new Image();img.onerror=reject;img.onload=()=>{const max=512,scale=Math.min(1,max/Math.max(img.width,img.height)),w=Math.max(1,Math.round(img.width*scale)),h=Math.max(1,Math.round(img.height*scale)),canvas=document.createElement('canvas');canvas.width=w;canvas.height=h;const ctx=canvas.getContext('2d');ctx.drawImage(img,0,0,w,h);resolve(canvas.toDataURL('image/jpeg',0.82))};img.src=reader.result};reader.readAsDataURL(file)})}

function enhancePatientModal(){const body=q('.modal-body'),id=q('#p-id');if(!body||!id)return;const p=patientById(id.value)||{},a=p.address||{};if(!q('#p-photo')){body.insertAdjacentHTML('afterbegin',`<div class="patient-photo-editor"><div id="p-photo-preview" class="patient-photo-preview"></div><div><div class="section-label">Foto do paciente</div><label class="btn secondary" for="p-photo">Selecionar foto</label><input id="p-photo" type="file" accept="image/*" hidden><div class="tiny muted mt-8">Imagem local, reduzida e armazenada junto ao cadastro deste paciente.</div></div></div>`);applyPhotoPreview(p.photo||'')}
if(!q('#p-cpf')){const firstRow=body.querySelector('.input-row');firstRow?.insertAdjacentHTML('afterend',`<div class="section-label mt-16">Identificação civil</div><div class="input-row"><div class="field"><label>CPF</label><input id="p-cpf" class="input" inputmode="numeric" placeholder="000.000.000-00" value="${escAttr(p.cpf||'')}"></div><div class="field"><label>RG</label><input id="p-rg" class="input" value="${escAttr(p.rg||'')}"></div></div>`) }
if(!q('#p-session-recurrence')){const valueField=q('#p-value')?.closest('.field');valueField?.insertAdjacentHTML('afterend',`<div class="field"><label>Frequência usual</label><select id="p-session-recurrence" class="select"><option ${!p.sessionRecurrence||p.sessionRecurrence==='Avulso'?'selected':''}>Avulso</option><option ${p.sessionRecurrence==='Semanal'?'selected':''}>Semanal</option><option ${p.sessionRecurrence==='Quinzenal'?'selected':''}>Quinzenal</option></select></div>`)}
if(!q('#p-zip')){const summary=q('#p-summary')?.closest('.field');summary?.insertAdjacentHTML('beforebegin',`<div class="section-label mt-16">Endereço completo</div><div class="input-row"><div class="field"><label>CEP</label><input id="p-zip" class="input" inputmode="numeric" placeholder="00000-000" value="${escAttr(a.zip||'')}"></div><div class="field"><label>Logradouro</label><input id="p-street" class="input" value="${escAttr(a.street||'')}"></div></div><div class="input-row mt-12"><div class="field"><label>Número</label><input id="p-number" class="input" value="${escAttr(a.number||'')}"></div><div class="field"><label>Complemento</label><input id="p-complement" class="input" value="${escAttr(a.complement||'')}"></div></div><div class="input-row mt-12"><div class="field"><label>Bairro</label><input id="p-neighborhood" class="input" value="${escAttr(a.neighborhood||'')}"></div><div class="field"><label>Cidade</label><input id="p-city" class="input" value="${escAttr(a.city||'')}"></div></div><div class="input-row mt-12"><div class="field"><label>UF</label><input id="p-state" class="input" maxlength="2" value="${escAttr(a.state||'')}"></div><div></div></div>`)}
const phone=q('#p-phone');if(phone){phone.placeholder='(00) 00000-0000';const label=phone.closest('.field')?.querySelector('label');if(label)label.textContent='Telefone';maskPhone(phone)}const value=q('#p-value');if(value&&!value.closest('.currency-field')){value.type='number';value.step='0.01';const wrap=document.createElement('div');wrap.className='currency-field';const span=document.createElement('span');span.textContent='R$';value.parentNode.insertBefore(wrap,value);wrap.append(span,value)}const foot=q('.modal-foot');if(foot&&!q('[data-action="clear-patient-draft"]'))foot.insertAdjacentHTML('afterbegin','<button type="button" class="btn ghost" data-action="clear-patient-draft">Descartar rascunho</button>');restoreDraft()}

function enhanceAppointmentModal(){const body=q('.modal-body'),id=q('#a-id');if(!body||!id||q('#a-recurrence'))return;const existing=arr(data.appointments).find(x=>x.id===id.value),patient=patientById(q('#a-patient')?.value||runtime.selectedPatientId),defaultRec=existing?.recurrence||patient?.sessionRecurrence||'Avulso';const note=q('#a-note')?.closest('.field');note?.insertAdjacentHTML('beforebegin',`<div class="section-label mt-16">Recorrência</div><div class="input-row"><div class="field"><label>Tipo</label><select id="a-recurrence" class="select" ${id.value?'disabled':''}><option ${defaultRec==='Avulso'||defaultRec==='Nenhuma'?'selected':''}>Avulso</option><option ${defaultRec==='Semanal'?'selected':''}>Semanal</option><option ${defaultRec==='Quinzenal'?'selected':''}>Quinzenal</option></select></div><div class="field"><label>Número de sessões</label><input id="a-recurrence-count" class="input" type="number" min="1" max="52" value="${existing?.occurrenceCount||1}" ${id.value?'disabled':''}></div></div><div class="tiny muted mt-8">Semanal repete a cada 7 dias; quinzenal, a cada 14 dias, sempre no mesmo horário. A agenda é atualizada ao salvar.</div>`);syncRecurrenceCount()}
function syncRecurrenceCount(){const r=q('#a-recurrence'),c=q('#a-recurrence-count');if(!r||!c)return;if(r.value==='Avulso'){c.value='1';c.disabled=true}else if(!q('#a-id')?.value){c.disabled=false;if(Number(c.value)<2)c.value='10'}}

async function savePatient(){const id=q('#p-id').value||uid('pat'),prior=patientById(id),obj={...(prior||{}),id,name:sanitizeText(q('#p-name').value),preferredName:sanitizeText(q('#p-preferred').value),cpf:sanitizeText(q('#p-cpf')?.value),rg:sanitizeText(q('#p-rg')?.value),code:sanitizeText(q('#p-code').value)||`PAC-${String(arr(data.patients).length+1).padStart(3,'0')}`,status:q('#p-status').value,birthDate:q('#p-birth').value,phone:sanitizeText(q('#p-phone').value),email:sanitizeText(q('#p-email').value),modality:q('#p-modality').value,value:Number(q('#p-value').value)||0,sessionRecurrence:q('#p-session-recurrence')?.value||prior?.sessionRecurrence||'Avulso',photo:q('#p-photo-preview')?.dataset.photo||prior?.photo||'',address:{zip:sanitizeText(q('#p-zip')?.value),street:sanitizeText(q('#p-street')?.value),number:sanitizeText(q('#p-number')?.value),complement:sanitizeText(q('#p-complement')?.value),neighborhood:sanitizeText(q('#p-neighborhood')?.value),city:sanitizeText(q('#p-city')?.value),state:sanitizeText(q('#p-state')?.value)},summary:sanitizeText(q('#p-summary').value),communicationPreferences:{...(prior?.communicationPreferences||{}),channel:q('#p-channel').value,whatsappConsent:q('#p-wa-consent').checked,emailConsent:q('#p-email-consent').checked,silentStart:prior?.communicationPreferences?.silentStart||'20:00',silentEnd:prior?.communicationPreferences?.silentEnd||'08:00'},createdAt:prior?.createdAt||nowISO()};validatePatient(obj);await saveEntity('patients',obj);await deletePatientDraft();runtime.selectedPatientId=id;closeModal();rerender('patients');toast('Cadastro salvo.','success')}
function conflict(base,date,ignore=''){const start=new Date(`${date}T${base.time}:00`).getTime(),end=start+base.duration*60000;return arr(data.appointments).find(x=>x.id!==ignore&&x.status!=='Cancelada'&&x.date===date&&(()=>{const s=new Date(`${x.date}T${x.time}:00`).getTime(),e=s+(Number(x.duration)||50)*60000;return start<e&&end>s})())}
async function saveAppointment(){const id=q('#a-id')?.value||'',patientId=q('#a-patient')?.value||runtime.selectedPatientId;assert(patientId,'Selecione explicitamente o paciente.');const base={patientId,date:q('#a-date').value,time:q('#a-time').value,duration:Number(q('#a-duration').value)||50,status:q('#a-status').value,modality:q('#a-modality').value,location:sanitizeText(q('#a-location').value),note:sanitizeText(q('#a-note').value)};if(id){const c=conflict(base,base.date,id);if(c)throw new Error(`Conflito de horário com ${patientById(c.patientId)?.name||'outro compromisso'}.`);const old=arr(data.appointments).find(x=>x.id===id)||{};await saveEntity('appointments',{...old,...base,id,createdAt:old.createdAt||nowISO()});closeModal();paintAgenda();toast('Horário salvo.','success');return}
const recurrence=q('#a-recurrence')?.value||'Avulso',count=Math.max(1,Math.min(52,Number(q('#a-recurrence-count')?.value)||1)),step=recurrence==='Semanal'?7:recurrence==='Quinzenal'?14:0,total=step?count:1,seriesId=total>1?uid('series'):'',dates=[];for(let i=0;i<total;i++){const d=new Date(`${base.date}T12:00:00`);d.setDate(d.getDate()+i*step);const iso=d.toISOString().slice(0,10),c=conflict(base,iso);if(c)throw new Error(`Conflito de horário em ${new Intl.DateTimeFormat('pt-BR').format(d)} com ${patientById(c.patientId)?.name||'outro compromisso'}.`);dates.push(iso)}for(let i=0;i<dates.length;i++)await saveEntity('appointments',{...base,id:uid('apt'),date:dates[i],recurrence,seriesId,occurrenceIndex:i+1,occurrenceCount:dates.length,createdAt:nowISO()});closeModal();paintAgenda();if(runtime.route==='patients')rerender('patients');toast(dates.length>1?`${dates.length} sessões ${recurrence.toLowerCase()}s agendadas.`:'Horário salvo.','success')}

function paintAgenda(){if(runtime.locked||runtime.route!=='agenda')return;const main=q('#main-content');if(!main)return;main.innerHTML=renderAppointments138();main.dataset.agendaUx=VERSION}
function fixFinanceIcon(){const nav=q('.nav-link[data-route="financeiro"]');if(!nav)return;const first=nav.firstElementChild;if(first?.tagName==='IMG'){first.src='assets/images/brand-symbol.svg';first.className='brain-icon';return}const img=document.createElement('img');img.src='assets/images/brand-symbol.svg';img.alt='';img.className='brain-icon';if(first)first.replaceWith(img);else nav.prepend(img)}
function tickClock(){const chip=q('#clock-chip');if(!chip)return;const now=new Date(),date=now.toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit',year:'numeric'}),time=now.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit',second:'2-digit'});chip.innerHTML=`<div><small>Data</small><strong>${date}</strong></div><div><small>Hora</small><strong>${time}</strong></div>`}
function enhanceCurrentUi(){enhancePatientModal();enhanceAppointmentModal();fixFinanceIcon();if(runtime.route==='agenda'&&q('#main-content')?.dataset.agendaUx!==VERSION)paintAgenda();tickClock()}

async function openOriginalAction(action,id=''){const b=document.createElement('button');b.dataset.action=action;if(id)b.dataset.id=id;b.hidden=true;document.body.append(b);b.click();b.remove();await new Promise(r=>setTimeout(r,0));enhanceCurrentUi()}

document.addEventListener('input',e=>{if(e.target.id==='p-phone')maskPhone(e.target);if(e.target.id==='p-cpf')maskCpf(e.target);if(e.target.id==='p-zip')maskCep(e.target);if(e.target.id==='p-state')e.target.value=e.target.value.toUpperCase().replace(/[^A-Z]/g,'').slice(0,2);if(e.target.closest('.modal')&&q('#p-id'))queuePatientDraft()},true);
document.addEventListener('change',async e=>{if(e.target.id==='a-patient'&&!q('#a-id')?.value){const p=patientById(e.target.value),r=q('#a-recurrence');if(r&&p?.sessionRecurrence)r.value=p.sessionRecurrence;syncRecurrenceCount()}if(e.target.id==='a-recurrence')syncRecurrenceCount();if(e.target.id==='p-photo'&&e.target.files?.[0]){try{const dataUrl=await compressPhoto(e.target.files[0]);applyPhotoPreview(dataUrl);queuePatientDraft()}catch{toast('Não foi possível processar a foto.','error')}}if(e.target.closest('.modal')&&q('#p-id'))queuePatientDraft()},true);
document.addEventListener('click',e=>{const a=e.target.closest('[data-action]')?.dataset.action;if(['new-patient','edit-patient','new-appointment','edit-appointment'].includes(a))setTimeout(enhanceCurrentUi,0);if(a==='remove-patient-photo'){e.preventDefault();applyPhotoPreview('');queuePatientDraft()}},false);

document.addEventListener('click',async e=>{const el=e.target.closest('[data-action]');if(!el||!customActions.has(el.dataset.action))return;e.preventDefault();e.stopImmediatePropagation();const a=el.dataset.action;try{
if(a==='save-patient')return await savePatient();
if(a==='save-appointment')return await saveAppointment();
if(a==='clear-patient-draft'){await deletePatientDraft();const id=q('#p-id')?.value||'';closeModal();await openOriginalAction(id?'edit-patient':'new-patient',id);toast('Rascunho descartado.','success');return}
if(a==='schedule-selected-patient'){assert(selectedPatient(),'Selecione um paciente.');return await openOriginalAction('new-appointment')}
if(a==='quick-schedule-patient'){runtime.selectedPatientId=el.dataset.id;assert(selectedPatient(),'Paciente não localizado.');return await openOriginalAction('new-appointment')}
if(a==='agenda-prev-week'){localStorage.setItem('rm.agenda.weekOffset',String(Number(localStorage.getItem('rm.agenda.weekOffset')||0)-1));paintAgenda();return}
if(a==='agenda-next-week'){localStorage.setItem('rm.agenda.weekOffset',String(Number(localStorage.getItem('rm.agenda.weekOffset')||0)+1));paintAgenda();return}
if(a==='agenda-today'){localStorage.setItem('rm.agenda.weekOffset','0');paintAgenda();return}
if(a==='delete-appointment'){const x=arr(data.appointments).find(v=>v.id===el.dataset.id);assert(x,'Agendamento não encontrado.');const p=patientById(x.patientId);if(!confirm(`Excluir o agendamento de ${p?.name||'paciente'} em ${fmtDate(x.date)} às ${x.time}?`))return;await removeEntity('appointments',x.id);paintAgenda();toast('Agendamento excluído.','success');return}
if(a==='delete-appointment-series'){const series=el.dataset.series,items=arr(data.appointments).filter(x=>x.seriesId===series);assert(items.length,'Série não encontrada.');if(!confirm(`Excluir toda a série recorrente com ${items.length} sessão(ões)?`))return;for(const item of [...items])await removeEntity('appointments',item.id);paintAgenda();toast('Série de agendamentos excluída.','success');return}
if(a==='appointment-whatsapp'){const x=arr(data.appointments).find(v=>v.id===el.dataset.id);assert(x,'Sessão não localizada.');const p=patientById(x.patientId);assert(p?.phone,'Cadastre o telefone do paciente.');openWhatsApp(p.phone,renderTemplate(DEFAULT_MESSAGES.sessionReminder,{primeiro_nome:firstName(p),data_sessao:fmtDate(x.date),hora_sessao:x.time,nome_profissional:preferences.professionalName,titulo_profissional:preferences.professionalTitle}));return}
if(a==='register-session-from-agenda'){const x=arr(data.appointments).find(v=>v.id===el.dataset.id);assert(x?.patientId,'Agendamento sem paciente vinculado.');const p=patientById(x.patientId);assert(p,'Paciente do agendamento não localizado.');runtime.selectedPatientId=p.id;runtime.patientTab='records';runtime.route='patients';location.hash='patients';window.dispatchEvent(new Event('hashchange'));setTimeout(()=>{const date=q('#record-date');if(date)date.value=x.date;const title=q('#record-title');if(title&&!title.value)title.value='Evolução de sessão';q('#patient-workspace')?.scrollIntoView({behavior:'smooth',block:'start'})},120);return}
if(a==='patient-whatsapp'){const p=selectedPatient();assert(p?.phone,'Cadastre o telefone do paciente.');openWhatsApp(p.phone,`Olá, ${firstName(p)}. ${preferences.professionalName} — ${preferences.professionalTitle}.`);return}
if(a==='patient-email'){const p=selectedPatient();assert(p?.email,'Cadastre o e-mail do paciente.');openEmail({to:p.email,subject:'Comunicação de acompanhamento',body:`Olá, ${firstName(p)}.\n\n${preferences.professionalName} — ${preferences.professionalTitle}.`});return}
if(a==='document-email'){const d=arr(data.documents).find(x=>x.id===el.dataset.id),p=selectedPatient();assert(d&&p?.email,'Documento ou e-mail não localizado.');openEmail({to:p.email,subject:'Documento disponível',body:`Olá, ${firstName(p)}. O documento “${d.type}” foi preparado. Anexe o PDF antes do envio.\n\n${preferences.professionalName} — ${preferences.professionalTitle}.`});return}
if(a==='clear-patient-local-data'){const p=selectedPatient();assert(p,'Selecione um paciente.');if(!confirm(`Excluir todos os dados locais vinculados a ${p.name}? Os demais pacientes serão preservados.`)||!confirm('Confirmação final: esta ação não pode ser desfeita sem backup.'))return;for(const store of ['appointments','records','notes','formulations','goals','tasks','materials','documents','payments','consents','communications','settings'])for(const item of arr(data[store]).filter(x=>x.patientId===p.id))await removeEntity(store,item.id);await removeEntity('patients',p.id);runtime.selectedPatientId=null;runtime.patientTab='summary';rerender('patients');toast('Dados deste paciente excluídos.','success');return}
}catch(err){toast(err.message||'Não foi possível concluir a ação.','error');console.error(err)}},true);

const observer=new MutationObserver(()=>{enhanceCurrentUi()});observer.observe(document.documentElement,{subtree:true,childList:true});window.addEventListener('hashchange',()=>setTimeout(enhanceCurrentUi,0));setInterval(tickClock,1000);setTimeout(enhanceCurrentUi,150);
