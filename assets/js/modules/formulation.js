import {data,selectedPatient} from '../state.js';
import {empty,esc} from '../ui.js';
export const FORM_FIELDS=[['demand','Demanda atual'],['targets','Problemas-alvo'],['history','História relevante'],['predisposing','Fatores predisponentes'],['precipitating','Fatores precipitantes'],['maintaining','Fatores de manutenção'],['protective','Fatores protetivos'],['resources','Recursos'],['coreBeliefs','Crenças centrais'],['intermediateBeliefs','Crenças intermediárias'],['compensatory','Estratégias compensatórias'],['automaticThoughts','Pensamentos recorrentes'],['emotions','Emoções predominantes'],['behaviors','Padrões comportamentais'],['schemas','Esquemas'],['modes','Modos'],['needs','Necessidades emocionais'],['hypotheses','Hipóteses clínicas'],['interventions','Intervenções possíveis'],['toVerify','Dados a verificar']];
export const CYCLE=['Situação','Interpretação','Emoção','Resposta corporal','Comportamento','Consequência imediata','Consequência tardia','Manutenção do padrão'];
export function renderFormulation(){const p=selectedPatient();if(!p)return empty('Paciente não selecionado','Selecione um paciente para abrir a conceitualização.');const f=data.formulations.find(x=>x.patientId===p.id)||{fields:{},cycle:{}};return`<section class="grid grid-2"><div class="card"><h3>Conceitualização estruturada</h3><div class="grid grid-2">${FORM_FIELDS.map(([id,label])=>`<div class="field"><label>${label}</label><textarea class="textarea formulation-field" data-field="${id}">${esc(f.fields?.[id]||'')}</textarea></div>`).join('')}</div><button class="btn mt-16" data-action="save-formulation">Salvar nova versão</button></div><div class="card"><h3>Ciclo de manutenção</h3><div class="cycle">${CYCLE.map((label,i)=>`<div class="cycle-card"><label class="small"><strong>${label}</strong></label><textarea class="textarea cycle-field" data-cycle="${i}" style="min-height:80px">${esc(f.cycle?.[i]||'')}</textarea></div>${i<CYCLE.length-1?'<div class="cycle-arrow">↓</div>':''}`).join('')}</div><button class="btn secondary mt-16" data-action="print">Imprimir mapa</button></div></section>`}
