import {data,selectedPatient,todayISO} from '../state.js';
import {empty,esc,fmtDate,badge} from '../ui.js';
const arr=v=>Array.isArray(v)?v.filter(Boolean):[];
function sessionId(a){return a?.clinicalSessionId||a?.id||''}
function recordForAppointment(patientId,a){const sid=sessionId(a);return arr(data.records).filter(r=>r?.patientId===patientId&&(r?.appointmentId===a?.id||r?.clinicalSessionId===sid)).sort((x,y)=>String(y.updatedAt||y.createdAt||'').localeCompare(String(x.updatedAt||x.createdAt||'')))[0]||null}
function currentAppointment(patientId){const today=todayISO();return arr(data.appointments).filter(a=>a?.patientId===patientId&&a?.date&&a.date<=today&&a.status!=='Cancelada'&&a.attendanceStatus!=='Desmarcou').sort((a,b)=>`${b.date}${b.time||''}`.localeCompare(`${a.date}${a.time||''}`))[0]||null}
function planFor(patientId){return arr(data.notes).find(n=>n?.patientId===patientId&&n?.kind==='therapeuticActionPlan')?.plan||null}
function bullets(values=[]){return arr(values).slice(0,5).map(v=>`<li>${esc(v)}</li>`).join('')}
export function renderSessionFlow(){
  const p=selectedPatient();if(!p)return empty('Paciente não selecionado','Selecione explicitamente um paciente antes de iniciar o atendimento.');
  const records=arr(data.records).filter(r=>r?.patientId===p.id&&r?.text).sort((a,b)=>`${b.date||''}${b.time||''}`.localeCompare(`${a.date||''}${a.time||''}`));
  const lastFinal=records.find(r=>r.status==='Finalizado')||records[0]||null,currentAppt=currentAppointment(p.id),currentRecord=currentAppt?recordForAppointment(p.id,currentAppt):records[0]||null;
  const tasks=arr(data.tasks).filter(t=>t?.patientId===p.id&&!['Concluída'].includes(t.status)),goals=arr(data.goals).filter(g=>g?.patientId===p.id&&g.status!=='Alcançado'),next=arr(data.appointments).filter(a=>a?.patientId===p.id&&a.date>=todayISO()&&a.status!=='Cancelada').sort((a,b)=>(a.date+a.time).localeCompare(b.date+b.time))[0],plan=planFor(p.id);
  const autoSummary=currentRecord?.sessionSummary||currentRecord?.text||'',autoFollowup=currentRecord?.followup||lastFinal?.followup||'',planNext=arr(plan?.nextActions).slice(0,5);
  return`<div class="stepper rm-session-flow-v320">${['Preparação','Sessão','Registro','Plano e encaminhamentos','Continuidade'].map((x,i)=>`<button class="step ${i===0?'active':''}" data-action="session-step" data-step="${i}">${i+1}. ${x}</button>`).join('')}</div>
  <section class="grid grid-2"><div class="card"><div class="eyebrow">1. Preparação</div><h3>${esc(p.preferredName||p.name)}</h3><p><strong>Sessão em foco:</strong> ${currentAppt?`${fmtDate(currentAppt.date)} · ${esc(currentAppt.time||'—')}`:'sem sessão identificada'}</p><p><strong>Registro anterior:</strong> ${lastFinal?fmtDate(lastFinal.date):'sem registro finalizado'}</p><p class="muted">${esc((lastFinal?.text||'Não há síntese anterior disponível.').slice(0,650))}</p>${plan?.objectives?.length?`<h4 class="mt-16">Objetivos terapêuticos</h4><ul>${bullets(plan.objectives)}</ul>`:''}${plan?.resources?.length?`<h4 class="mt-16">Recursos/proteções</h4><ul>${bullets(plan.resources)}</ul>`:''}</div>
  <div class="card"><div class="eyebrow">2. Sessão</div><h3>Informações já disponíveis</h3>${autoSummary?`<div class="notice"><strong>Prontuário/Gemini desta sessão</strong><p class="small mt-8">${esc(autoSummary.slice(0,1100))}</p></div>`:'<p class="muted">Ainda não há conteúdo associado a esta sessão.</p>'}<div class="field mt-12"><label>Pauta trazida pelo paciente</label><textarea id="session-patient-agenda" class="textarea"></textarea></div><div class="field mt-12"><label>Prioridades do psicólogo</label><textarea id="session-professional-agenda" class="textarea">${esc(planNext.join('\n'))}</textarea></div><div class="field mt-12"><label>Pontos para retomada</label><textarea id="session-followup" class="textarea">${esc(autoFollowup)}</textarea></div><button class="btn mt-16" data-action="generate-session-agenda">Gerar pauta estruturada</button><div id="generated-agenda" class="notice mt-12 hidden"></div></div></section>
  <section class="grid grid-2 mt-16"><div class="card"><div class="eyebrow">3. Registro</div><h3>Prontuário da sessão</h3><p class="small muted">O editor de prontuário é vinculado à sessão e faz gravação cifrada com conferência pós-salvamento.</p><div class="flex gap-8 wrap"><button class="btn" data-action="patient-tab" data-tab="records">Abrir prontuário</button>${currentRecord?`<span class="badge">${esc(currentRecord.status||'Rascunho')}</span>`:''}</div></div>
  <div class="card"><div class="eyebrow">4. Plano e encaminhamentos</div><h3>Conduta longitudinal</h3>${plan?`<p class="small muted">Plano assistido atualizado a partir da Anamnese, objetivos e prontuários.</p>${planNext.length?`<ul>${bullets(planNext)}</ul>`:''}`:'<p class="muted">O plano será estruturado quando houver Anamnese, objetivos ou prontuários disponíveis.</p>'}<div class="flex gap-8 wrap"><button class="btn secondary" data-action="patient-tab" data-tab="plans">Plano terapêutico</button><button class="btn secondary" data-action="patient-tab" data-tab="tasks">Tarefa</button><button class="btn secondary" data-action="patient-tab" data-tab="materials">Material</button></div></div></section>
  <section class="card mt-16"><div class="card-title"><div><div class="eyebrow">5. Continuidade</div><h3 class="mb-0">Próxima sessão e comunicação</h3></div><span class="badge">${next?`${fmtDate(next.date)} ${esc(next.time)}`:'Sem próxima sessão'}</span></div><div class="flex gap-8 wrap mt-12"><button class="btn secondary" data-action="schedule-selected-patient">Agendar sessão</button><button class="btn secondary" data-action="patient-tab" data-tab="communications">Comunicação</button><button class="btn secondary" data-action="patient-tab" data-tab="finance">Financeiro</button></div></section>`
}
