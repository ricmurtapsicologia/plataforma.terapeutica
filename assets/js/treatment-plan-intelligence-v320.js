import {data,runtime,selectedPatient,nowISO} from './state.js';
import {bulkPutEncryptedAtomic} from './database.js';
import {esc,toast} from './ui.js';
import {FIELD} from './clinical-intake-core-v310.mjs';

const VERSION='3.6.0';
const KIND='therapeuticActionPlan';
const arr=v=>Array.isArray(v)?v.filter(Boolean):[];
const pending=new Map();let worker=null;
function clean(v=''){return String(v||'').replace(/\r/g,'').replace(/\s+/g,' ').trim()}
function uniq(values){const seen=new Set(),out=[];for(const raw of values){const v=clean(raw),k=v.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();if(!v||seen.has(k))continue;seen.add(k);out.push(v)}return out}
function intakeFacts(note){const map=new Map();for(const section of arr(note?.intake?.sections))for(const item of arr(section.items)){const field=String(item?.field||''),value=clean(item?.patientReportedFact||'');if(field&&value){if(!map.has(field))map.set(field,[]);map.get(field).push(value)}}return map}
function facts(map,...fields){return uniq(fields.flatMap(field=>map.get(field)||[]))}
function assessment(patientId){return arr(data.notes).find(n=>n?.patientId===patientId&&n?.kind==='clinicalAssessmentAddendum')?.fields||{}}
function formulation(patientId){return arr(data.formulations).find(f=>f?.patientId===patientId)||null}
function recordSummary(r){return clean(r?.sessionSummary||r?.text||'').slice(0,900)}
function bulletsText(value=''){return uniq(String(value||'').split(/\n|•|;/).map(x=>x.replace(/^[-–]\s*/,'')))}
function fieldValues(f,key){return bulletsText(f?.fields?.[key]||'')}
function formulationFromSources(map,f,a){
  return{
    predisposing:uniq([...fieldValues(f,'predisposing'),...facts(map,FIELD.childhood,FIELD.caregivers,FIELD.emotionalResponse,FIELD.feelingsSpace,FIELD.earlyMaturity,FIELD.formativeExperience)]),
    precipitating:uniq([...fieldValues(f,'precipitating'),...facts(map,FIELD.reason,FIELD.currentDifficulty,FIELD.triggers),...bulletsText(a.antecedents)]),
    maintaining:uniq([...fieldValues(f,'maintaining'),...facts(map,FIELD.responsePattern,FIELD.coping,FIELD.selfTalk,FIELD.repeatedPatterns,FIELD.relationalDifficulty),...bulletsText(a.maintenance),...bulletsText(a.safetyAvoidance),...bulletsText(a.consequences)]),
    protective:uniq([...fieldValues(f,'protective'),...fieldValues(f,'resources'),...facts(map,FIELD.strengths,FIELD.priorResources,FIELD.support,FIELD.helps),...bulletsText(a.resources)]),
    automaticThoughts:uniq([...fieldValues(f,'automaticThoughts'),...facts(map,FIELD.selfTalk)]),
    behaviors:uniq([...fieldValues(f,'behaviors'),...facts(map,FIELD.responsePattern,FIELD.coping)]),
    hypotheses:uniq(fieldValues(f,'hypotheses')),
    evidenceFor:uniq(fieldValues(f,'evidenceFor')),
    evidenceAgainst:uniq(fieldValues(f,'evidenceAgainst')),
    confidence:uniq(fieldValues(f,'hypothesisConfidence')),
    toVerify:uniq([...fieldValues(f,'toVerify'),...bulletsText(a.clinicalHypotheses)])
  }
}
function structuredTargets(goals){return goals.map(g=>({id:g.id,problem:g.title||'',formulation:g.formulationLink||g.description||'',baseline:g.baseline||'',target:g.target||'',intervention:g.intervention||'',homework:g.homework||'',indicator:g.indicators||'',review:g.reviewDate||'',decision:g.decision||'A definir',status:g.status||'Planejado'}))}
function buildPlan(patientId){
  const intakes=arr(data.notes).filter(n=>n?.patientId===patientId&&n?.kind==='clinicalIntake').sort((a,b)=>String(b?.source?.submittedAt||b.createdAt||'').localeCompare(String(a?.source?.submittedAt||a.createdAt||''))),latest=intakes[0]||null,map=intakeFacts(latest),a=assessment(patientId),f=formulation(patientId),records=arr(data.records).filter(r=>r?.patientId===patientId&&clean(r.text||r.sessionSummary)&&r?.source?.type!=='missing-record-placeholder'&&clean(r.text)!=='Não há prontuário.').sort((x,y)=>`${y.date||''}${y.time||''}`.localeCompare(`${x.date||''}${x.time||''}`)).slice(0,14),goals=arr(data.goals).filter(g=>g?.patientId===patientId&&!['Alcançado'].includes(g.status)).sort((x,y)=>Number(x.priority||99)-Number(y.priority||99)),form=formulationFromSources(map,f,a);
  const problemTargets=uniq([...bulletsText(a.problemList),...facts(map,FIELD.reason,FIELD.currentDifficulty,FIELD.impactAreas,FIELD.repeatedPatterns,FIELD.relationalDifficulty),...fieldValues(f,'targets'),...goals.map(g=>g.title)]),objectives=uniq([...bulletsText(a.patientGoals),...facts(map,FIELD.desiredChange,FIELD.expectations),...goals.map(g=>g.target||g.title)]),resources=uniq([...bulletsText(a.resources),...facts(map,FIELD.strengths,FIELD.priorResources,FIELD.support,FIELD.helps)]),barriers=uniq([...bulletsText(a.safetyAvoidance),...bulletsText(a.maintenance),...facts(map,FIELD.triggers,FIELD.responsePattern,FIELD.coping,FIELD.selfTalk)]),nextActions=uniq([...goals.map(g=>g.homework),...records.flatMap(r=>String(r.followup||'').split(/\n|•|;/))]).filter(Boolean).slice(0,12),evolution=records.slice(0,8).map(r=>({date:r.date||'',time:r.time||'',summary:recordSummary(r),followup:clean(r.followup||'')})),interventions=uniq([...goals.map(g=>g.intervention),...fieldValues(f,'interventions')]),indicators=uniq(goals.map(g=>g.indicators)),targets=structuredTargets(goals);
  if(!problemTargets.length&&!objectives.length&&!resources.length&&!records.length&&!form.predisposing.length&&!targets.length)return null;
  const assessmentRow=arr(data.notes).find(n=>n?.patientId===patientId&&n?.kind==='clinicalAssessmentAddendum'),sourceSignature=JSON.stringify({intakes:intakes.map(n=>[n.id,n.updatedAt||n?.source?.submittedAt||'',n?.intake?.sourceHash||'']),assessment:assessmentRow?[assessmentRow.id,assessmentRow.updatedAt||'']:null,formulation:f?[f.id,f.updatedAt||'',f.version||0]:null,records:records.map(r=>[r.id,r.updatedAt||'',r.status||'']),goals:goals.map(g=>[g.id,g.updatedAt||'',g.status||'',g.decision||'',g.baseline||'',g.target||'',g.intervention||'',g.indicators||''])});
  return{version:VERSION,generatedAt:nowISO(),basedOn:{intakes:intakes.map(n=>n.id),assessment:assessmentRow?.id||'',formulation:f?.id||'',records:records.map(r=>r.id),goals:goals.map(g=>g.id)},sourceSignature,problemTargets:problemTargets.slice(0,12),objectives:objectives.slice(0,12),resources:resources.slice(0,12),barriers:barriers.slice(0,12),formulation:form,targets,interventions:interventions.slice(0,12),indicators:indicators.slice(0,12),nextActions,evolution,reviewRequired:true,autonomousDiagnosis:false}
}
function textPlan(plan){const out=['PLANO TERAPÊUTICO — RASCUNHO ASSISTIDO','Referencial principal: Terapia Cognitivo-Comportamental','Status: revisão profissional necessária',''];const add=(title,list)=>{if(!list?.length)return;out.push(title);for(const x of list)out.push(`• ${x}`);out.push('')};add('1. DEMANDAS E PROBLEMAS-ALVO',plan.problemTargets);add('2. OBJETIVOS TERAPÊUTICOS',plan.objectives);add('3. RECURSOS E FATORES PROTETIVOS',plan.resources);add('4. BARREIRAS E PADRÕES RELEVANTES',plan.barriers);add('5. FATORES PREDISPONENTES',plan.formulation?.predisposing);add('6. FATORES PRECIPITANTES',plan.formulation?.precipitating);add('7. FATORES DE MANUTENÇÃO',plan.formulation?.maintaining);add('8. HIPÓTESES DE TRABALHO',plan.formulation?.hypotheses);add('9. EVIDÊNCIAS FAVORÁVEIS',plan.formulation?.evidenceFor);add('10. EVIDÊNCIAS CONTRÁRIAS',plan.formulation?.evidenceAgainst);if(plan.targets.length){out.push('11. OBJETIVOS E INDICADORES ESTRUTURADOS');for(const g of plan.targets)out.push(`• ${g.problem} | baseline: ${g.baseline||'—'} | meta: ${g.target||'—'} | intervenção: ${g.intervention||'—'} | tarefa: ${g.homework||'—'} | indicador: ${g.indicator||'—'} | decisão: ${g.decision}`);out.push('')}add('12. PRÓXIMOS PASSOS',plan.nextActions);if(plan.evolution.length){out.push('13. EVOLUÇÃO RECENTE');for(const e of plan.evolution)out.push(`• ${e.date||'Sessão'}${e.time?` ${e.time}`:''}: ${e.summary}${e.followup?` | Retomar: ${e.followup}`:''}`);out.push('')}out.push('Revisão clínica: confirmar prioridades, mecanismos, metas e indicadores; registrar evidências que fortalecem ou enfraquecem a formulação e atualizar a decisão terapêutica.');out.push('Este documento organiza dados existentes. Não cria diagnóstico nem substitui decisão clínica profissional.');return out.join('\n')}
function seedFormulationObject(patientId,plan){const existing=formulation(patientId);if(existing&&!existing.autoSeeded)return null;const fields={...(existing?.fields||{})},put=(key,values)=>{if(!clean(fields[key])&&values?.length)fields[key]=values.map(x=>`• ${x}`).join('\n')};put('demand',plan.problemTargets);put('targets',plan.problemTargets);put('predisposing',plan.formulation?.predisposing);put('precipitating',plan.formulation?.precipitating);put('maintaining',plan.formulation?.maintaining);put('protective',plan.formulation?.protective);put('resources',plan.resources);put('automaticThoughts',plan.formulation?.automaticThoughts);put('behaviors',plan.formulation?.behaviors);put('hypotheses',plan.formulation?.hypotheses);put('evidenceFor',plan.formulation?.evidenceFor);put('evidenceAgainst',plan.formulation?.evidenceAgainst);put('toVerify',plan.formulation?.toVerify);put('interventions',plan.interventions);const cycle={...(existing?.cycle||{})};if(!clean(cycle[0])&&plan.formulation?.precipitating?.length)cycle[0]=plan.formulation.precipitating.join('\n');if(!clean(cycle[1])&&plan.formulation?.automaticThoughts?.length)cycle[1]=plan.formulation.automaticThoughts.join('\n');if(!clean(cycle[4])&&plan.formulation?.behaviors?.length)cycle[4]=plan.formulation.behaviors.join('\n');if(!clean(cycle[7])&&plan.formulation?.maintaining?.length)cycle[7]=plan.formulation.maintaining.join('\n');return{...(existing||{}),id:existing?.id||`form_assisted_${patientId}`,patientId,fields,cycle,version:Number(existing?.version||0)+1,autoSeeded:true,requiresReview:true,createdAt:existing?.createdAt||nowISO(),updatedAt:nowISO()}}
async function refreshPatient(patientId,{render=true}={}){if(runtime.locked||!runtime.key||!runtime.dataReady||!patientId)return null;const plan=buildPlan(patientId);if(!plan)return null;const id=`treatment_plan_assisted_${patientId}`,old=arr(data.notes).find(n=>n?.id===id),text=textPlan(plan),writes=[];let row=old;if(!(old?.sourceSignature===plan.sourceSignature&&old?.text===text)){row={...(old||{}),id,patientId,kind:KIND,title:'Plano terapêutico TCC — assistido',status:'Rascunho assistido',reviewRequired:true,autonomousDiagnosis:false,plan,text,sourceSignature:plan.sourceSignature,createdAt:old?.createdAt||nowISO(),updatedAt:nowISO()};writes.push({storeName:'notes',values:[row]})}const form=seedFormulationObject(patientId,plan);if(form)writes.push({storeName:'formulations',values:[form]});if(writes.length)await bulkPutEncryptedAtomic(writes,runtime.key,{verify:true});if(row&&row!==old){const i=data.notes.findIndex(n=>n.id===id);if(i>=0)data.notes[i]=row;else data.notes.push(row)}if(form){const i=data.formulations.findIndex(f=>f.patientId===patientId);if(i>=0)data.formulations[i]=form;else data.formulations.push(form)}if(writes.length)document.dispatchEvent(new CustomEvent('rm:treatment-plan-updated',{detail:{patientId,id,at:nowISO()}}));if(render)window.__rmRender?.();return row||old}
function currentPlan(patientId){return arr(data.notes).find(n=>n?.patientId===patientId&&n?.kind===KIND)||null}
function listBlock(title,items){if(!items?.length)return'';return`<div class="mt-16"><strong>${esc(title)}</strong><ul class="mt-8">${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`}
function targetBlock(targets){if(!targets?.length)return'';return`<div class="mt-16"><strong>Objetivos estruturados</strong><div class="list mt-8">${targets.slice(0,6).map(g=>`<div class="list-item"><div><strong>${esc(g.problem)}</strong><div class="small muted">Meta: ${esc(g.target||'—')} · Indicador: ${esc(g.indicator||'—')} · Decisão: ${esc(g.decision||'A definir')}</div></div></div>`).join('')}</div></div>`}
function decoratePlans(){if(runtime.locked||runtime.route!=='patients'||runtime.patientTab!=='plans')return;const p=selectedPatient(),host=document.getElementById('patient-tab-content');if(!p||!host)return;host.querySelector('#rm-treatment-plan-v320')?.remove();const note=currentPlan(p.id),plan=note?.plan;if(!plan)return;const card=document.createElement('section');card.id='rm-treatment-plan-v320';card.className='card mb-16';card.innerHTML=`<div class="card-title"><div><div class="eyebrow">Plano longitudinal · TCC</div><h3 class="mb-0">Plano terapêutico</h3></div><span class="badge">Rascunho assistido</span></div><p class="small muted mt-12">Atualizado a partir da Anamnese, complementação profissional, Formulação TCC, objetivos e prontuários. Revise antes de adotar como plano clínico.</p>${listBlock('Demandas e problemas-alvo',plan.problemTargets)}${listBlock('Objetivos terapêuticos',plan.objectives)}${listBlock('Recursos e fatores protetivos',plan.resources)}${listBlock('Fatores de manutenção',plan.formulation?.maintaining)}${listBlock('Hipóteses a testar',plan.formulation?.hypotheses)}${targetBlock(plan.targets)}${listBlock('Próximos passos',plan.nextActions)}<div class="flex gap-8 wrap mt-16"><button class="btn secondary" data-rm-plan-refresh="1">Atualizar agora</button><button class="btn secondary" data-action="patient-tab" data-tab="formulation">Abrir Formulação TCC</button></div>`;host.prepend(card)}
async function drain(){if(worker)return worker;worker=(async()=>{while(pending.size){const[patientId,render]=pending.entries().next().value;pending.delete(patientId);try{await refreshPatient(patientId,{render:false})}catch(err){console.warn('Plano terapêutico assistido:',err)}if(render)decoratePlans()}})().finally(()=>{worker=null});return worker}
function schedule(patientId='',delay=250,{render=false}={}){const id=patientId||runtime.selectedPatientId||'';if(!id)return;pending.set(id,Boolean(render)||Boolean(pending.get(id)));setTimeout(()=>void drain(),delay)}
async function refreshAll(){for(const p of arr(data.patients))if(p?.id&&p.status!=='Profissional')pending.set(p.id,false);await drain();decoratePlans()}
document.addEventListener('click',e=>{if(!e.target.closest?.('[data-rm-plan-refresh]'))return;e.preventDefault();const p=selectedPatient();if(!p)return;void refreshPatient(p.id).then(()=>toast('Plano terapêutico e Formulação TCC atualizados para revisão.','success')).catch(err=>toast(err.message||'Não foi possível atualizar o plano.','error'))},true);
document.addEventListener('rm:data-ready',()=>setTimeout(()=>void refreshAll(),1200));document.addEventListener('rm:data-patched',()=>setTimeout(()=>void refreshAll(),700));document.addEventListener('rm:local-data-changed',e=>{if(!['records','notes','goals','formulations'].includes(e.detail?.storeName)&&!String(e.detail?.kind||'').startsWith('clinical-intake')&&!String(e.detail?.kind||'').startsWith('clinical-data-integrity'))return;const p=selectedPatient();if(p)schedule(p.id,300,{render:true});else setTimeout(()=>void refreshAll(),450)});document.addEventListener('rm:record-verified-save',e=>schedule(e.detail?.patientId||'',220,{render:true}));document.addEventListener('rm:rendered',()=>decoratePlans());globalThis.__rmTreatmentPlan={version:VERSION,refresh:refreshPatient,refreshAll,current:currentPlan,build:buildPlan};
